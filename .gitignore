# Secretos y configuración local — nunca deben subirse
.env
.env.*

# Configuración local de Claude Code — puede contener claves reales
# pegadas en el historial de comandos permitidos
.claude/settings.local.json

# Dependencias (se reinstalan con npm install)
node_modules/

# Carpeta y zip de salida del empaquetado .exe (se regeneran con el build)
dist/
MonitorPokemon.zip
MonitorPokemon-assets.zip

# Bases de datos locales
*.db

# Estado/caché generado en tiempo de ejecución (se reconstruye solo)
heartbeat_message_ids.json
mensaje_id.txt
mensaje_prueba_id.txt
pm2list.json
stats_cache.json
assets/drive_cache/
assets/drive_folder_map.json
assets/drive_cache_gold/
assets/drive_folder_map_gold.json
assets/guild_emojis_cache.json
assets/wallpapers_cache/
drive_hd_errores.txt
update_fallo.txt
automation/Logs/
logs/
.pending_update.json
.pending_restart.json
_update.bat
_update_panel.bat
MonitorPokemon.new.exe
MonitorPokemonPanel.new.exe
assets-actualizacion.zip
.monitor.lock
.reconfigurar.lock
.panel.lock
Open configuration.url
Change token or API key.lnk
Monitor Pokemon.lnk
Ports in use.txt

# Panel de control compilado sueltos en la raiz (uso local de desarrollo) -
# la version real que se distribuye sale de dist/, generada por build-exe.js
MonitorPokemonPanel.exe

# Binario de terceros (cloudflared.exe, ~54MB) para el tunel publico de Info
# Accounts -- no tiene sentido versionarlo en el repo de codigo fuente (pesa
# demasiado y no cambia). Si existe localmente en bin/, scripts/build-exe.js
# lo copia igual al paquete distribuido (dist/bin), asi el tunel funciona
# para todos, no solo en esta PC.
bin/

# keep the documented template tracked (it holds no secrets)
!.env.example

# build outputs copied to the repo root for gh release upload
MonitorPokemon.exe
MonitorPokemonPanel.exe

# runtime-refreshed copy of card_types.json (the tracked one is the shipped fallback)
assets/card_types.local.json
